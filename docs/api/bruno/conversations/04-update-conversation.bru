meta {
  name: UPDATE Conversation
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/conversations/{{conversationId}}
  body: json
  auth: none
}

body:json {
  {
    "title": "Título atualizado"
  }
}
