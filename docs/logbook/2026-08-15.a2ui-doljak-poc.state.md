---
spec: a2ui-doljak-poc.state
date: 08-15-2026
repo: doljak-projects/A2UI_A17_Python
repo_url: https://github.com/doljak-projects/A2UI_A17_Python
description: Monorepo A2UI: frontend Angular 17 + Angular Material e backend Python (FastAPI).
generated_by: claude-code / doljak-update-myself
---

# Project State — A2UI · 08-15-2026

## References

| Resource | URL / Path |
|---|---|
| GitHub Repo | https://github.com/doljak-projects/A2UI_A17_Python |
| CLAUDE.md | a2ui-2026.05.27/CLAUDE.md |
| Logbook anterior | a2ui-2026.05.27/docs/logbook/2026-08-01.a2ui-doljak-poc.state.md |

---

## Session Summary · 08-15-2026 — Sessão 1

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Retomada da issue #59 (migração Angular 17→21) na worktree `wts/59-worktree-angular-upgrade`, branch `chore/angular-upgrade-59-17-to-22`.
- Escrito um spec sucinto (`spec-cursor-59-passo2-3.md`, no scratchpad) pra orientar o Cursor a confirmar o Passo 2 (18→19) e executar o Passo 3 (19→20).
- Verificação independente do trabalho feito pelo Cursor: `git log`/`git status` conferidos, diffs de `angular.json`/`tsconfig.json`/`package.json` inspecionados, e `ng build` + `ng test` rodados manualmente pra confirmar — **build limpo (481.28 kB)** e **`ng test` 36/39** (mesmas 3 falhas pré-existentes de `chat.component.spec.ts`, sem regressão). Passo 2 já estava commitado (`41e265f`); Passo 3 estava certo, aplicado mas não commitado, como o spec pedia.
- Decisão explícita do usuário: **não fazer o Passo 4 (20→21) por enquanto** — ficar na v20 e ir direto pro Passo 5 (validação final). Consequência registrada: nessa versão `@copilotkit/angular` (issues #46–50) já é compatível (`^20`), mas `@a2ui/angular` (issues #52–55, Parte 3) continua bloqueado, pois exige `^21.2.5`.
- Checkpoint do Passo 3 commitado (`chore: upgrade Angular 19 -> 20 (checkpoint da migração #59)`, commit `166355f`), pushado, e PR **#60** aberta (`https://github.com/doljak-projects/A2UI_A17_Python/pull/60`) — issue #59 mantida **aberta**.
- Sincronização de rotina: doc `docs/issues-plans/45-Back-weather-tool-agent-post.md` (issue já fechada desde 2026-08-01) estava com `status: ready-for-review` — corrigido para `status: closed`.

### Combinados para continuar
- Passos 4 (20→21, se for retomado no futuro) e 5 (validação final da issue #59) serão feitos **no Cursor**, fora desta sessão.
- PR #60 aguarda merge manual do usuário quando ele decidir.
- Issues #52–55 (Parte 3, SDK A2UI da Google) seguem bloqueadas enquanto o projeto ficar na v20 — só desbloqueiam se decidir subir pra v21 depois.
- Issues #46–50 (Parte 2, CopilotKit) já estão tecnicamente desbloqueadas na v20 — podem ser retomadas assim que a PR #60 for mergeada e o Passo 5 confirmar que tudo segue funcional.

## Session Summary · 08-15-2026 — Sessão 2

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Usuário confirmou merge da PR #60 e informou que a issue #59 estava "fechada" — verificação mostrou que a PR foi mergeada, mas a issue ainda estava `OPEN` no GitHub (não tinha `Closes #59` no corpo, por decisão da sessão anterior).
- Descoberto, via `git log`/`gh pr list`, que **PR #61** (`chore: upgrade Angular 17 -> 21 (#59)`, commit `6cf864f`, co-autoria Cursor) foi mergeada separadamente — o usuário completou os Passos 4 (20→21) e 5 (validação final) no Cursor, como combinado, e mergeou direto. `apps/frontend` está oficialmente em **Angular 21.2.19** (Material/CDK `^21.2.14`, TypeScript `~5.9.2`).
- Confirmado (`npm view`) que `@copilotkit/angular@0.3.1` e `@a2ui/angular@0.10.5` (versões atuais do registry) satisfazem os peer ranges do Angular 21 — as duas trilhas de SDK (Parte 2 e Parte 3) estão desbloqueadas de fato.
- Issue #59 fechada nesta sessão via `gh issue close`, com comentário explicando o histórico (checkpoint v20 → decisão de completar até v21 no Cursor).
- `main` local atualizado via `git pull --ff-only` — trouxe os commits das PRs #60 e #61. Docs `docs/tutorial_A2UI/02-ag-ui-copilotkit-angular.md` e `03-google-a2ui-dynamic-ui.md` já vieram com `status: draft` e as seções de bloqueio removidas (o próprio Cursor cuidou disso na PR #61).
- Doc `docs/issues-plans/59-Chore-angular-upgrade.md` confirmado com `status: closed` e os 5 passos marcados `[x]`.

### Combinados para continuar
- Issue #59 encerrada de ponta a ponta — projeto agora roda em Angular 21.
- Próximo passo natural do tutorial: retomar as issues #46 (instalar CopilotKit) e #52 (instalar SDK A2UI da Google), agora ambas desbloqueadas — sem worktree dedicada ainda, a criar quando uma delas for iniciada.
- Nenhum outro combinado pendente desta sessão.

---

## In Development

> Issues com label `developing` — trabalho ativo. Nenhuma issue tem a label `developing` no momento e nenhuma tem worktree dedicada.

> ⚠️ As issues abaixo (#46–50, #52–55) estão todas **desbloqueadas** desde o fechamento da #59 (Angular 21) — nenhuma tem worktree dedicada ainda.

### #46 — [Front] -AG-UI- Install and configure CopilotKit for Angular
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/46
- **Resumo:** Instalar `@copilotkit/angular` e registrar `provideCopilotKit({ defaultToolRendering: true })`.

### #47 — [Front] -AG-UI- Agent store: AppHttpAgent, initAgentStore and injectAgentStore
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/47
- **Resumo:** `AppHttpAgent` (subclasse de `HttpAgent`) + `initAgentStore` (registro no runtime do CopilotKit) + `injectWeatherAgentStore()`, apontando pro endpoint da #45.

### #48 — [Front] -A2UI- Weather frontend tool via createFrontendTool
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/48
- **Resumo:** Reimplementa a tool client-side de clima (#35/#36) via `createFrontendTool` — CopilotKit executa o `handler` sozinho, sem ciclo manual de duas runs.

### #49 — [Front] -A2UI- Weather widget rendered via copilot-render-tool-calls
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/49
- **Resumo:** `WeatherWidget` (`ToolRenderer<WeatherToolResult>`) anexado à tool de clima, renderizado como card no histórico do chat.

### #50 — [Front] -A2UI- Sidecar chat UI wired to the CopilotKit agent store
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/50
- **Resumo:** UI de chat de demonstração isolada, ligada ao agent store via `sendMessage()`.

### #52 — [Front] -GOOGLE-A2UI- Install and configure the A2UI Angular SDK
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/52
- **Resumo:** Instalar `@a2ui/angular`/`@a2ui/web_core` e registrar `A2UI_RENDERER_CONFIG`/`BasicCatalog`/`provideMarkdownRenderer`. Trilha independente da Parte 2.

### #53 — [Front] -GOOGLE-A2UI- Render a static card via createSurface/updateComponents/updateDataModel
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/53
- **Resumo:** Helper que devolve `A2uiMessage[]`, processado via `A2uiRendererService`, exibido via `<a2ui-v09-surface>` numa rota de demo isolada.

### #54 — [Front] -A2UI- Model the A2UI weather card using the existing WeatherToolResult shape
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/54
- **Resumo:** Reaproveita `WeatherToolResult`/`weatherSchema` (issue #35) como dado do card A2UI, em vez de um modelo novo.

### #55 — [Front] -GOOGLE-A2UI- Handle a client action to refresh the weather card in place
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/55
- **Resumo:** `Button` + `renderer.surfaceGroup.onAction` pra atualizar o card via `updateDataModel`, sem recriar a surface.

---

## Closed Issues

> Ordenadas por data de fechamento, mais recente primeiro.

### #59 — [Front] [Chore] Upgrade Angular from 17 to 21 (unblocks CopilotKit/A2UI Angular SDKs)
- **Fechada em:** 2026-08-15
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/59-Chore-angular-upgrade.md`
- **Resumo:** Migração sequencial do frontend 17→18→19→20→21 via `ng update`. PR #60 (checkpoint até v20) + PR #61 (Passos 4/5, feitos no Cursor, completando até v21). Desbloqueia `@copilotkit/angular` (#46–50) e `@a2ui/angular` (#52–55).

### #45 — [Back] -AG-UI- Accept a real RunAgentInput via POST for a resumable weather tool-call agent
- **Fechada em:** 2026-08-01
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/45-Back-weather-tool-agent-post.md`
- **Resumo:** Endpoint `POST /api/agui/weather-tool-agent-demo` aceitando `RunAgentInput` real; agente resumível que emite a tool call pendente ou confirma o resultado por texto, resolvendo a limitação de transporte GET-only da issue #36.

### #36 — [Front] -A2UI- Two-run cycle for client-side weather tool call result
- **Fechada em:** 2026-08-01
- **Resumo:** Ciclo de duas `runAgent()` calls montado à mão pra resolver uma tool call client-side de clima.

### #35 — [Front] -A2UI- Define client-side weather Tool schema for chat rendering
- **Fechada em:** 2026-08-01
- **Resumo:** Schema `WeatherToolResult`/`weatherSchema` (Zod) definido pro domínio de clima, reaproveitado em issues posteriores.

### #34 — [Front] -AG-UI- Consume agent events via HttpAgent and AgentSubscriber
- **Fechada em:** 2026-08-01
- **Resumo:** Consumo de eventos do agente via `HttpAgent` + `AgentSubscriber` manual no frontend.

### #33 — [Back] -A2UI- Wire existing get_weather tool as an AG-UI server-side tool call
- **Fechada em:** 2026-07-28
- **Resumo:** `get_weather` exposta como tool call server-side no protocolo AG-UI.

### #32 — [Back] -AG-UI- Agent skeleton emitting RUN_STARTED/TEXT_MESSAGE/RUN_FINISHED events
- **Fechada em:** 2026-07-28
- **Resumo:** Esqueleto de agente AG-UI emitindo o ciclo básico de eventos.

### #26 — FE: Rota /chat e link de navegação na toolbar
- **Fechada em:** 2026-07-25
- **Resumo:** Rota `/chat` e link de navegação adicionados à toolbar.

### #25 — FE: ChatComponent — layout, estado com Signals e renderização de stream
- **Fechada em:** 2026-07-25
- **Resumo:** `ChatComponent` com layout, estado via Signals e renderização de stream de mensagens.

### #24 — FE: ChatService — consumir POST /api/chat via SSE (fetch streaming)
- **Fechada em:** 2026-07-25
- **Resumo:** `ChatService` consumindo `POST /api/chat` via SSE com `fetch` streaming.

### #14 — [Backend] Migrate weather integration from OpenWeatherMap to WeatherAPI.com
- **Fechada em:** 2026-07-25
- **Resumo:** Migração da integração de clima de OpenWeatherMap para WeatherAPI.com.

### #9 — [Chore] Organize worktree structure and update README
- **Fechada em:** 2026-07-25
- **Resumo:** Estrutura de worktrees organizada e README atualizado.

### #8 — BE: Implementar CRUD end-to-end de entidade de exemplo
- **Fechada em:** 2026-07-25
- **Resumo:** CRUD completo de entidade de exemplo implementado no backend.

### #7 — BE: Expor tools via MCP Server no FastAPI
- **Fechada em:** 2026-07-25
- **Resumo:** Tools expostas via MCP Server integrado ao FastAPI.

### #6 — BE: Estruturar camada de tools chamáveis pelo LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Camada de tools chamáveis pelo LLM estruturada no backend.

### #5 — BE: Criar tool get_weather(city) consumindo a OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Tool `get_weather(city)` criada consumindo a OpenWeatherMap (posteriormente migrada na issue #14).

### #4 — BE: Registrar e configurar chave da OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Chave da OpenWeatherMap registrada e configurada via settings.

### #3 — BE: Tratar streaming de eventos LLM no backend
- **Fechada em:** 2026-07-25
- **Resumo:** Streaming de eventos do LLM tratado no backend.

### #2 — BE: Implementar tool calling / function calling com o LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Tool calling / function calling implementado com o LLM.

### #1 — BE: Configurar provedor LLM via .env e pydantic-settings
- **Fechada em:** 2026-07-25
- **Resumo:** Provedor LLM configurado via `.env` e `pydantic-settings`.
