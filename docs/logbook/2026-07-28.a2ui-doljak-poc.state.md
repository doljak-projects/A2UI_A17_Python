---
spec: a2ui.ag.ui.state
date: 2026-07-28
repo: doljak-projects/A2UI_A17_Python
repo_url: https://github.com/doljak-projects/A2UI_A17_Python
description: Monorepo A2UI: frontend Angular 17 + Angular Material e backend Python (FastAPI).
generated_by: claude-code / doljak-update-myself
---

# Project State — A2UI · 2026-07-28

## References

| Resource | URL / Path |
|---|---|
| GitHub Repo | https://github.com/doljak-projects/A2UI_A17_Python |
| Local frontend dev | http://localhost:4200 (`npm run fe:start`) |
| Local backend dev | http://localhost:8000 (`npm run be:start`, docs em `/docs`) |
| CLAUDE.md | a2ui-2026.05.27/CLAUDE.md |
| Tutorial A2UI (Parte 1) | a2ui-2026.05.27/docs/tutorial_A2UI/01-ag-ui-sdk-typescript.md |
| Estado anterior | a2ui-2026.05.27/docs/logbook/2026-07-27.a2ui-doljak-poc.state.md |

---

## Session Summary · 2026-07-28 — Sessão 1

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Decisão de arquitetura para as issues `[Back]` do tutorial AG-UI: implementar em **Python** (backend real do projeto), usando o pacote oficial `ag-ui-protocol` (PyPI), em vez de introduzir um runtime Node só para replicar o SDK TypeScript do artigo original — confirmado que o AG-UI usa HTTP+SSE (não WebSockets) e que o próprio artigo cita SDK oficial em Python.
- Documentada, no doc da issue #32, uma tabela de equivalência TS → Python (`AbstractAgent` → classe abstrata própria; `Observable<BaseEvent>` → async/sync generator; `EventType`/`BaseEvent` → `ag_ui.core`; SSE → `ag_ui.encoder.EventEncoder`). Essa decisão vale como padrão para as próximas issues `[Back]` do tutorial.
- Implementação da issue **#32** na worktree `wts/32-worktree-agui-agent` (branch `feat/agui-agent-32-skeleton-events`): `app/agui/agent.py` (`AGUIAgent` + `WeatherChatAgent`), dependência `ag-ui-protocol` adicionada ao `requirements.txt`, `tests/test_agui_agent.py` (5 testes). Suíte completa do backend: 92 passed, `ruff check` limpo. Ainda **não commitado**.
- Nova convenção de nomenclatura para docs de `docs/issues-plans/` definida pelo usuário, específica deste projeto: `<numero-issue>-<Front|Back>-<ag-ui|a2ui>-<tema>.md`. Skill global `doljak-create-issue` (`~/.claude/commands/doljak-create-issue.md`) atualizada para reconhecer convenções de nomenclatura específicas de projeto, sem generalizar as tags `ag-ui`/`a2ui`.
- Doc da issue #32 renomeado de `issue-32-agui-agent-skeleton.md` para `32-Back-ag-ui-agent-skeleton.md`.

### Combinados para continuar
- Revisar o código da #32 antes de commitar/abrir PR (usuário ainda não pediu commit).
- Seguir para a issue **#33** (`[Back] -A2UI- Wire existing get_weather tool as an AG-UI server-side tool call`) reaproveitando a base da #32, aplicando a mesma decisão de arquitetura (Python + ag-ui-protocol).

---

## Session Summary · 2026-07-28 — Sessão 2

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Teste funcional da issue #32: além dos testes automatizados, foi criada a rota de demo `GET /api/agui/demo` (`app/api/routes/agui.py`) para validar o agente ponta a ponta via curl/Swagger, já que a issue original não previa endpoint HTTP. Suíte subiu para 94 passed.
- Commit e PR #38 da issue #32 (branch `feat/agui-agent-32-skeleton-events`, `Closes #32`), mesclada — issue #32 **fechada**.
- Worktree `wts/32-worktree-agui-agent` removida após o merge; worktree/branch nova criada para a issue **#33** (`wts/33-worktree-agui-weather-tool`, branch `feat/agui-weather-tool-33-server-side-call`), baseada na `main` atualizada.
- Implementação da issue #33: `WeatherToolCallAgent(AGUIAgent)` estendendo o esqueleto da #32, emitindo `TOOL_CALL_START/ARGS/END` e resolvendo com o `get_weather` real (issue #5, sem mock); rota de demo `GET /api/agui/weather-tool-demo`; tratamento de erro adicionado (`RunErrorEvent`/`RUN_ERROR`) depois de observar, via curl manual sem `WEATHER_API_KEY` válida, que o stream cortava sem sinalizar falha. Suíte subiu para 104 passed, `ruff check` limpo.
- README atualizado: itens de #32 e #33 marcados como concluídos no checklist AG-UI/A2UI.
- Commit e PR #39 da issue #33 (`Closes #33`) aberta — **ainda não mesclada**.
- Correção da skill global `doljak-update-myself` (`~/.claude/commands/doljak-update-myself.md`): removido o hardcode "waggy" (resquício de outro projeto), agora deriva a tag do projeto de arquivos `*.state.md` existentes ou pergunta na primeira execução. Arquivos de estado deste projeto renomeados de `*.waggy.state.md` para `*.a2ui.ag.ui.state.md`, com `spec:` corrigido no frontmatter.

### Combinados para continuar
- Aguardar o merge da PR #39 (issue #33).
- Depois do merge, seguir para as issues do lado frontend do tutorial: **#34** (`[Front] -AG-UI- Consume agent events via HttpAgent and AgentSubscriber`), **#35** (`[Front] -A2UI- Define client-side weather Tool schema for chat rendering`) e **#36** (`[Front] -A2UI- Two-run cycle for client-side weather tool call result`).

---

## In Development

> Issues com label `developing` — trabalho ativo.

_(nenhuma issue com label `developing` no momento — nota: a issue #33 está com implementação concluída e PR aberta, mas sem essa label aplicada no GitHub)_

### #33 — [Back] -A2UI- Wire existing get_weather tool as an AG-UI server-side tool call
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/33
- **PR:** https://github.com/doljak-projects/A2UI_A17_Python/pull/39 (aberta, `Closes #33`)
- **Worktree:** `a2ui-2026.05.27/wts/33-worktree-agui-weather-tool`
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/33-Back-a2ui-weather-tool-call.md`
- **Resumo:** `WeatherToolCallAgent` estende o esqueleto da #32 emitindo uma tool call server-side (`TOOL_CALL_START/ARGS/END/RESULT`) resolvida com o `get_weather` real. Implementado, testado (104 passed) e com PR aberta aguardando merge.

---

## Closed Issues

> Ordenadas por data de fechamento, mais recente primeiro.

### #32 — [Back] -AG-UI- Agent skeleton emitting RUN_STARTED/TEXT_MESSAGE/RUN_FINISHED events
- **Fechada em:** 2026-07-28
- **Resumo:** Agente Python (`WeatherChatAgent`, base `AGUIAgent`) emitindo `RUN_STARTED → TEXT_MESSAGE_START/CONTENT/END → RUN_FINISHED` via `ag-ui-protocol`, sem LLM. Rota de demo `GET /api/agui/demo` para teste funcional.

### #26 — FE: Rota /chat e link de navegação na toolbar
- **Fechada em:** 2026-07-25
- **Resumo:** Adiciona rota lazy `/chat` em `app.routes.ts` e link de navegação na toolbar Material do `AppComponent`, com `routerLinkActive`.

### #25 — FE: ChatComponent — layout, estado com Signals e renderização de stream
- **Fechada em:** 2026-07-25
- **Resumo:** Componente standalone de chat com estado gerenciado via Signals (`messages`, `isStreaming`, `error`), renderizando tokens do stream SSE em tempo real.

### #24 — FE: ChatService — consumir POST /api/chat via SSE (fetch streaming)
- **Fechada em:** 2026-07-25
- **Resumo:** Serviço Angular que consome `POST /api/chat` via Fetch API + ReadableStream, parseando eventos SSE tipados (`text_delta`, `message_stop`, `error`).

### #14 — [Backend] Migrate weather integration from OpenWeatherMap to WeatherAPI.com
- **Fechada em:** 2026-07-25
- **Resumo:** Migra cliente HTTP, settings e testes de OpenWeatherMap para WeatherAPI.com, ajustando `get_current_weather` ao endpoint `/current.json`.

### #9 — [Chore] Organize worktree structure and update README
- **Fechada em:** 2026-07-25
- **Resumo:** Padroniza nomenclatura de worktrees (`<numero>-worktree-<tema>`) e documenta o workflow baseado em worktrees no README.

### #8 — BE: Implementar CRUD end-to-end de entidade de exemplo
- **Fechada em:** 2026-07-25
- **Resumo:** Estabelece o padrão de camadas do backend (model, schema, service, router) com CRUD completo de `Conversation`, incluindo migração e testes de integração.

### #7 — BE: Expor tools via MCP Server no FastAPI
- **Fechada em:** 2026-07-25
- **Resumo:** Monta servidor MCP em `app/mcp/server.py` mapeando as tools do `ToolRegistry`, integrado ao lifecycle do FastAPI.

### #6 — BE: Estruturar camada de tools chamáveis pelo LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Cria `ToolRegistry` central com registro, schema JSON e execução uniforme de tools em `app/tools/registry.py`.

### #5 — BE: Criar tool get_weather(city) consumindo a OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa `get_weather(city)` em `app/services/weather.py`, registrada na camada de tools, retornando temperatura, descrição, umidade e cidade.

### #4 — BE: Registrar e configurar chave da OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Adiciona configuração e cliente HTTP (httpx) para a OpenWeatherMap API em `Settings` e `.env.example`.

### #3 — BE: Tratar streaming de eventos LLM no backend
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa `POST /api/chat` com `StreamingResponse`, emitindo cada delta de texto do LLM como evento SSE.

### #2 — BE: Implementar tool calling / function calling com o LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa o contrato de tool calling do LLM: montagem do payload com tools disponíveis e tratamento de `tool_use`/`function_call`.

### #1 — BE: Configurar provedor LLM via .env e pydantic-settings
- **Fechada em:** 2026-07-25
- **Resumo:** Integra configuração do provedor LLM (`llm_provider`, `llm_api_key`, `llm_model` etc.) ao `Settings` via pydantic-settings.
