---
spec: a2ui-doljak-poc.state
date: 2026-08-01
repo: doljak-projects/A2UI_A17_Python
repo_url: https://github.com/doljak-projects/A2UI_A17_Python
description: Monorepo A2UI: frontend Angular 17 + Angular Material e backend Python (FastAPI).
generated_by: claude-code / doljak-update-myself
---

# Project State — A2UI · 2026-08-01

## References

| Resource | URL / Path |
|---|---|
| GitHub Repo | https://github.com/doljak-projects/A2UI_A17_Python |
| Local worktree (main) | /Volumes/Singularity_4DK/Sites/projetos.study/A2UI/a2ui-2026.05.27 |
| CLAUDE.md | a2ui-2026.05.27/CLAUDE.md |
| Docs de tutorial AG-UI | a2ui-2026.05.27/docs/tutorial_A2UI/01-ag-ui-sdk-typescript.md |
| Estado anterior | a2ui-2026.05.27/docs/logbook/2026-07-28.a2ui-doljak-poc.state.md |

---

## Session Summary · 2026-08-01 — Sessão 1

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Nenhum tópico relevante registrado — sessão dedicada apenas à geração deste arquivo de estado.

---

## Session Summary · 2026-08-01 — Sessão 2

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Renomeação dos docs de `docs/issues-plans/` para o padrão `<numero>-<Tipo>-<titulo>.md` (Back/Front/Chore), incluindo a issue #9; branch `chore/rename-issue-docs-standard-pattern` criada, commitada e mergeada via PR #40.
- Sincronização da worktree da issue #34 (`wts/34-worktree-agui-http-agent`) com `main`, sem conflitos.
- Implementação completa da issue #34 em modo mentorado: `AguiGetHttpAgent` (override de `requestInit()` para GET), `AguiAgentService` (DI, thread compartilhada), `aguiLogSubscriber` (handlers de texto/execução, sem tool call — fora do escopo), `AguiTestComponent` + rota `/agui-test` (ponto de entrada isolado do `ChatComponent`/`ChatService`).
- Testes nas 3 camadas para os novos arquivos AG-UI (8/8 verdes); suíte completa 26/29 (3 falhas pré-existentes em `chat.component.spec.ts`, não relacionadas).
- Validação manual local: backend (`uvicorn`) + frontend (`ng serve`) rodados, `curl` no endpoint `weather-tool-demo` e teste no navegador via `/agui-test`, ambos confirmados com sucesso.
- Doc da issue #34 expandido com seção `## Decisões de implementação`, registrando o racional de cada passo (override do SDK, motivo do DI, SRP do subscriber, compatibilidade com o tutorial).
- Convenção de "modo mentorado + registrar decisões no doc da issue" salva em memória para as próximas sessões deste projeto.
- Branch `feat/agui-http-agent-34-consume-events` commitada, pushada e PR #41 aberta com `Closes #34` — mergeada; issue #34 fechada.
- Servidores locais (backend `:8000`, frontend `:4200`) encerrados ao final.
- Correção de local do arquivo de estado: arquivos anteriores (`07-27`, `07-28`, e a primeira tentativa de `08-01`) haviam sido gerados na pasta pai `A2UI/` com tags inconsistentes (`a2ui`, `a2ui.ag.ui`) — divergência introduzida em sessões anteriores a partir de 07-27. Corrigido: local correto é `a2ui-2026.05.27/docs/logbook/`, tag `a2ui-doljak-poc` (convenção original de 07-25). Os 3 arquivos antigos foram movidos e renomeados para o padrão correto; nenhum conteúdo foi perdido.

### Combinados para continuar
- Próximos passos do tutorial AG-UI (frontend) seguem no modo mentorado: issue #35 (schema `Tool` client-side de clima) e #36 (ciclo de duas runs para tool call client-side), ainda sem worktree dedicada.

---

## Session Summary · 2026-08-01 — Sessão 3

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Ajuste de formato de data nos arquivos de estado: de `MM-DD-YYYY` (e uma tentativa intermediária por extenso, "August 1, 2026") para ISO `YYYY-MM-DD`, em nome de arquivo, frontmatter e títulos — padrão explicitamente pedido pelo usuário. Branch `docs/logbook-consolidation` (a mesma da correção de local do logbook) recebeu esse ajuste como commit adicional; PR #42 segue aberta, aguardando review (bloqueada por branch protection).
- Worktree `wts/35-worktree-weather-tool-schema` criada (branch `feat/weather-tool-schema-35-client-side-definition`) a partir da `main` atualizada; deps do frontend instaladas.
- Doc da issue #35 criado (`docs/issues-plans/35-Front-weather-tool-schema.md`), mesmo formato mentorado da #34.
- Implementação completa da issue #35 em modo mentorado: `zod` promovido de dependência transitiva (via `@ag-ui/client`) para dependência direta em `apps/frontend/package.json`; `weather-tool-for-a2ui.ts` criado com `weatherSchema` (campos `city`/`temperature_c`/`description`/`humidity`, batendo com o JSON real do backend, não com os nomes do tutorial), `showWeatherTool` (via `z.toJSONSchema(...)`, importado de `zod/v4` já que o `zod` instalado empacota v3+v4) e `parseWeatherToolResult(content)`. `AguiTestComponent` (da #34) passou a registrar `showWeatherTool` em `runAgent({ tools: [...] })`.
- Testes: `weather-tool-for-a2ui.spec.ts` (novo) + `agui-test.component.spec.ts` (ajustado) — 10/10 verdes nos arquivos da issue; suíte completa 32/35 (as mesmas 3 falhas pré-existentes de `chat.component.spec.ts`).
- Validação manual feita pelo próprio usuário (backend + frontend locais, `/agui-test` → "Rodar agente" → sem erros no console).
- Doc da #35 expandido com `## Decisões de implementação` (zod direto, nome do arquivo `weather-tool-for-a2ui.ts`, nomes de campo do backend, `zod/v4`).
- Commit, push e PR #43 abertos para a issue #35 (`Closes #35`) — **PR aberta, bloqueada por branch protection (review obrigatória), issue #35 continua aberta no GitHub** até o merge.
- Worktrees `wts/34-worktree-agui-http-agent` (já mergeada) e `wts/35-worktree-weather-tool-schema` (branch pushada, PR aberta) removidas a pedido do usuário — nenhum trabalho foi perdido, ambas as branches seguem íntegras no remoto.

### Combinados para continuar
- PRs #42 (consolidação do logbook) e #43 (issue #35) abertas, aguardando aprovação/merge do usuário — branch protection da `main` exige review.
- Depois do merge da #43, seguir para a issue #36 (ciclo de duas runs para tool call client-side), ainda sem worktree dedicada.

---

## Session Summary · 2026-08-01 — Sessão 4

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Usuário aprovou/mergeou manualmente as PRs #42 (logbook) e #43 (issue #35) no GitHub; `main` local sincronizada — issue #35 confirmada fechada.
- Worktree `wts/36-worktree-weather-tool-two-run` criada (branch `feat/weather-tool-two-run-36-client-side-cycle`) a partir da `main` atualizada; deps do frontend instaladas, `.env`/venv do backend copiados.
- Doc da issue #36 criado (`docs/issues-plans/36-Front-weather-tool-two-run.md`), mesmo formato mentorado das #34/#35.
- Decisão de arquitetura relevante: os endpoints de demo existentes resolvem a tool call inteiramente no servidor — nenhum deixa uma tool call pendente. Criado `WeatherClientToolCallAgent` (backend) + rota `GET /api/agui/weather-tool-client-demo`, emitindo `TOOL_CALL_START/ARGS/END` para `show_weather` sem `TOOL_CALL_RESULT`. "Execução client-side" decidida pelo usuário: servidor manda os args (`city`), cliente monta um resultado mockado a partir deles (preparando pra args mais complexos em etapas futuras).
- Limitação descoberta e documentada: como a `AguiGetHttpAgent` (#34) é `GET` sem corpo, o backend nunca vê de fato o `addMessage` do cliente antes da 2ª run — decisão do usuário: 2ª run aponta para `/agui/demo` (texto simples), só pra demonstrar visualmente que uma segunda chamada acontece.
- Implementação completa da issue #36 em modo mentorado: `createWeatherToolCallCapture()` e `buildMockWeatherResult(city)` em `weather-tool-for-a2ui.ts`; `AguiAgentService.pointAt(path)` pra trocar a URL da instância compartilhada entre as duas runs; `AguiTestComponent.runAgent()` estendido pro ciclo completo (1ª run → captura da tool call → resultado mockado → `addMessage` → 2ª run).
- Testes: backend `test_agui_weather_client_tool_agent.py` (5) + `test_agui_weather_client_tool_endpoint.py` (2), 111/111 na suíte completa, `ruff check` limpo. Frontend: `weather-tool-for-a2ui.spec.ts` (+3), `agui-agent.service.spec.ts` (+1), `agui-test.component.spec.ts` (reescrito) — 17/17 verdes nos arquivos da issue; suíte completa 36/39 (mesmas 3 falhas pré-existentes de `chat.component.spec.ts`).
- Validação manual feita pelo próprio usuário (ciclo de duas runs confirmado no navegador).
- Commit, push e PR #44 abertos para a issue #36 (`Closes #36`) — **PR aberta, bloqueada por branch protection (review obrigatória), issue #36 continua aberta no GitHub** até o merge.
- Worktree `wts/36-worktree-weather-tool-two-run` removida a pedido do usuário — branch íntegra no remoto.

### Combinados para continuar
- PR #44 (issue #36) aberta, aguardando aprovação/merge do usuário.
- Depois do merge, todas as issues do Tutorial A2UI Parte 1 (#32–#36) estarão fechadas — próximo passo seria avaliar a Parte 2 do tutorial (mencionada nas notas de 07-27: "AG-UI End to End: Connecting Server and Client"), ainda não documentada.

---

## Session Summary · 2026-08-01 — Sessão 5

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Usuário aprovou/mergeou a PR #44 no GitHub — issue #36 fechada. Tutorial A2UI Parte 1 (#32–#36) **100% concluído**.
- Usuário indicou o próximo artigo da série ("Agentic UI with Angular, CopilotKit, and AG-UI", artigo 4 de 10 da série "Agentic UI with Angular", mesmo autor — não confundir com "AG-UI End to End", que era artigo 3 e foi pulado por indicação do usuário). Conteúdo lido via WebFetch: CopilotKit (`@copilotkit/angular`) abstrai o boilerplate manual das issues #34–#36 — agent store baseado em Signals (`initAgentStore`/`injectAgentStore`), `createFrontendTool` (execução automática de tool, sem `AgentSubscriber` manual nem ciclo de duas runs), e widgets (componente Angular anexado a uma tool, renderizado via `<copilot-render-tool-calls>`). O artigo original usa domínio de voos com backend Mastra (Node) — adaptado para o domínio de clima já estabelecido, já que o A2UI usa backend Python (decisão da issue #32).
- Criado `docs/tutorial_A2UI/02-ag-ui-copilotkit-angular.md` (Parte 2 do tutorial), mesmo formato da Parte 1, com 6 passos didáticos.
- Criadas as issues **#45–#50** no GitHub: #45 `[Back]` endpoint `POST` real que finalmente resolve a limitação GET-only documentada na #36 (permite o backend enxergar de fato o resultado da tool call na 2ª run); #46 `[Front]` instalar/configurar CopilotKit; #47 `[Front]` agent store (`AppHttpAgent`/`initAgentStore`/`injectAgentStore`); #48 `[Front]` tool de clima via `createFrontendTool` (substitui o ciclo manual das #35/#36); #49 `[Front]` widget de clima (`ToolRenderer` + `copilot-render-tool-calls`); #50 `[Front]` chat sidecar ligado ao agent store.
- `README.md` atualizado: Parte 1 marcada como concluída (todos os itens `[x]`), checklist da Parte 2 adicionado com links pras issues #45–#50.
- Branch `docs/tutorial-a2ui-part2-copilotkit` criada, commit único (README + doc da Parte 2 + logbook), push e PR #51 aberta — **sem `Closes`, PR não fecha nenhuma issue** (é planejamento/criação de issues, não implementação).

### Combinados para continuar
- PR #51 aberta, aguardando aprovação/merge do usuário.
- Depois do merge, seguir para a issue #45 (backend, primeiro passo da Parte 2 — pré-requisito pras issues de frontend #46–#50).

---

## Session Summary · 2026-08-01 — Sessão 6

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Usuário indicou o artigo 5 da série ("A2UI: How AI Generates Dynamic UIs at Runtime") como próximo tema. Conteúdo lido via WebFetch: protocolo **A2UI da Google** (Agent-to-UI) — vocabulário de componentes predefinidos ("catálogo") + formato de mensagens (`createSurface`/`updateComponents`/`updateDataModel`/`deleteSurface`) pra um agente descrever UI sem o cliente carregar código estranho em runtime. Nome coincide com o do projeto A2UI, mas são coisas sem relação — usuário escolheu o marcador `-GOOGLE-A2UI-` pra distinguir das issues `-A2UI-` (integração específica do projeto).
- Criado `docs/tutorial_A2UI/03-google-a2ui-dynamic-ui.md` (Parte 3 do tutorial), com aviso de nomenclatura explícito logo no topo.
- Criadas as issues **#52–#55**: #52 instalar/configurar o SDK A2UI (`@a2ui/angular`, `@a2ui/web_core`); #53 renderizar card estático via mensagens do protocolo (rota de demo isolada); #54 (marcada `-A2UI-`, não `-GOOGLE-A2UI-`) reaproveitar `WeatherToolResult`/`weatherSchema` (issue #35) como dado do card, em vez de inventar um modelo novo; #55 ação client-side (`onAction`, não é Observable RxJS — cleanup via `DestroyRef`) pra atualizar o card no lugar.
- `README.md` atualizado com o checklist da Parte 3.
- Branch `docs/tutorial-a2ui-part3-google-a2ui` criada, commit único, push e PR #56 aberta (sem `Closes`) — **mergeada** pelo usuário.
- Confirmado com o usuário: dentro de cada parte a ordem numérica das issues é a ordem de dependência real (45→50 e 52→55); as duas partes (2 e 3) são trilhas independentes entre si — Parte 3 não depende de nada da Parte 2, só reaproveita a #35.

### Combinados para continuar
- Nenhuma issue com worktree dedicada ainda. Próximo passo: começar a issue #45 (backend, pré-requisito da Parte 2) em modo mentorado.

---

## In Development

> Issues com label `developing` — trabalho ativo.

_(nenhuma issue com label `developing` no momento, nenhuma worktree dedicada ainda)_

### #45 — [Back] -AG-UI- Accept a real RunAgentInput via POST for a resumable weather tool-call agent
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/45
- **Resumo:** Endpoint `POST /api/agui/weather-tool-agent-demo` que aceita um `RunAgentInput` real e resolve o ciclo de tool call client-side pra valer (fecha a lacuna deixada pela #36). Pré-requisito das issues #46–#50.

### #46 — [Front] -AG-UI- Install and configure CopilotKit for Angular
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/46
- **Resumo:** Instalar `@copilotkit/angular` e registrar `provideCopilotKit({ defaultToolRendering: true })`.

### #47 — [Front] -AG-UI- Agent store: AppHttpAgent, initAgentStore and injectAgentStore
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/47
- **Resumo:** `AppHttpAgent` (subclasse de `HttpAgent`) + `initAgentStore` (registro no runtime do CopilotKit) + `injectWeatherAgentStore()`, apontando pro endpoint da #45.

### #48 — [Front] -A2UI- Weather frontend tool via createFrontendTool
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/48
- **Resumo:** Reimplementa a tool client-side de clima (#35/#36) via `createFrontendTool` — CopilotKit executa o `handler` sozinho, sem ciclo manual de duas runs.

### #49 — [Front] -A2UI- Weather widget rendered via copilot-render-tool-calls
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/49
- **Resumo:** `WeatherWidget` (`ToolRenderer<WeatherToolResult>`) anexado à tool de clima, renderizado como card no histórico do chat.

### #50 — [Front] -A2UI- Sidecar chat UI wired to the CopilotKit agent store
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/50
- **Resumo:** UI de chat de demonstração isolada, ligada ao agent store via `sendMessage()`.

### #52 — [Front] -GOOGLE-A2UI- Install and configure the A2UI Angular SDK
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/52
- **Resumo:** Instalar `@a2ui/angular`/`@a2ui/web_core` e registrar `A2UI_RENDERER_CONFIG`/`BasicCatalog`/`provideMarkdownRenderer`. Trilha independente da Parte 2.

### #53 — [Front] -GOOGLE-A2UI- Render a static card via createSurface/updateComponents/updateDataModel
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/53
- **Resumo:** Helper que devolve `A2uiMessage[]`, processado via `A2uiRendererService`, exibido via `<a2ui-v09-surface>` numa rota de demo isolada.

### #54 — [Front] -A2UI- Model the A2UI weather card using the existing WeatherToolResult shape
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/54
- **Resumo:** Reaproveita `WeatherToolResult`/`weatherSchema` (issue #35) como dado do card A2UI, em vez de um modelo novo.

### #55 — [Front] -GOOGLE-A2UI- Handle a client action to refresh the weather card in place
- **Issue:** https://github.com/doljak-projects/A2UI_A17_Python/issues/55
- **Resumo:** `Button` + `renderer.surfaceGroup.onAction` pra atualizar o card via `updateDataModel`, sem recriar a surface.

---

## Closed Issues

> Ordenadas por data de fechamento, mais recente primeiro.

### #36 — [Front] -A2UI- Two-run cycle for client-side weather tool call result
- **Fechada em:** 2026-08-01
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/36-Front-weather-tool-two-run.md`
- **Resumo:** Ciclo completo de tool call client-side (2 runs): `WeatherClientToolCallAgent` (backend, tool call pendente) + captura/execução mockada/segunda run (frontend), com a limitação de transporte GET-only documentada para resolução futura (issue #45).

### #35 — [Front] -A2UI- Define client-side weather Tool schema for chat rendering
- **Fechada em:** 2026-08-01
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/35-Front-weather-tool-schema.md`
- **Resumo:** Schema `zod` (`weatherSchema`) e `Tool` (`showWeatherTool`) client-side de clima, com nomes de campo do backend real (`city`/`temperature_c`/`description`/`humidity`); registrado em `runAgent({ tools: [...] })` via `AguiTestComponent`.

### #34 — [Front] -AG-UI- Consume agent events via HttpAgent and AgentSubscriber
- **Fechada em:** 2026-08-01
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/34-Front-ag-ui-http-agent.md`
- **Resumo:** Instanciou `HttpAgent` no Angular (via subclasse `AguiGetHttpAgent`, GET sem corpo), montou um `AgentSubscriber` com handlers para os eventos de texto/execução e criou um ponto de entrada de teste (`AguiTestComponent`) que dispara `addMessage` + `runAgent`, validando o transporte ponta a ponta.

### #33 — [Back] -A2UI- Wire existing get_weather tool as an AG-UI server-side tool call
- **Fechada em:** 2026-07-28
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/33-Back-a2ui-weather-tool-call.md`
- **Resumo:** Estendeu o esqueleto do agente AG-UI para emitir a sequência `TOOL_CALL_START/ARGS/END/RESULT` dentro do mesmo `run`, resolvendo a chamada com o `get_weather(city)` real do backend em vez de mockar o resultado.

### #32 — [Back] -AG-UI- Agent skeleton emitting RUN_STARTED/TEXT_MESSAGE/RUN_FINISHED events
- **Fechada em:** 2026-07-28
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/32-Back-ag-ui-agent-skeleton.md`
- **Resumo:** Implementou um agente mínimo compatível com AG-UI estendendo `AbstractAgent` do SDK TypeScript, emitindo a sequência `RUN_STARTED → TEXT_MESSAGE_* → RUN_FINISHED` sem chamada real de LLM ainda.

### #26 — FE: Rota /chat e link de navegação na toolbar
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/26-Front-chat-routing-nav-toolbar-link.md`
- **Resumo:** Adicionou a rota lazy-loaded `/chat` em `app.routes.ts` e um link de navegação na toolbar Material do `AppComponent`.

### #25 — FE: ChatComponent — layout, estado com Signals e renderização de stream
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/25-Front-chat-component-signals-stream.md`
- **Resumo:** Construiu o `ChatComponent` standalone com estado via Signals, lista de mensagens rolável e acúmulo incremental dos deltas de texto do stream SSE.

### #24 — FE: ChatService — consumir POST /api/chat via SSE (fetch streaming)
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/24-Front-chat-service-sse-fetch.md`
- **Resumo:** Implementou `ChatService` consumindo o endpoint `POST /api/chat` via Fetch API com `ReadableStream`, parseando eventos SSE tipados (`text_delta`, `message_stop`, `error`).

### #14 — [Backend] Migrate weather integration from OpenWeatherMap to WeatherAPI.com
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/14-Back-migrate-weather-to-weatherapi.md`
- **Resumo:** Migrou o cliente HTTP, settings e testes de OpenWeatherMap para WeatherAPI.com, adaptando `get_current_weather` ao endpoint `/current.json` e mapeando os erros do provedor.

### #9 — [Chore] Organize worktree structure and update README
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/9-Chore-worktree-structure.md`
- **Resumo:** Estabeleceu e documentou o padrão de organização de worktrees, atualizando o README com a seção de workflow e convenção de nomes de branch.

### #8 — BE: Implementar CRUD end-to-end de entidade de exemplo
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/8-Back-crud-conversation-end-to-end.md`
- **Resumo:** Construiu um CRUD completo de uma entidade de exemplo (`Conversation`) — model, schema, service, router e testes de integração — estabelecendo o padrão de camadas do backend.

### #7 — BE: Expor tools via MCP Server no FastAPI
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/7-Back-expose-tools-via-mcp.md`
- **Resumo:** Implementou um MCP Server expondo as tools registradas no `ToolRegistry`, integrado ao ciclo de vida do FastAPI.

### #6 — BE: Estruturar camada de tools chamáveis pelo LLM
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/6-Back-structure-llm-callable-tools.md`
- **Resumo:** Criou a infraestrutura de registro, descoberta e execução uniforme de tools (`ToolRegistry`) no backend.

### #5 — BE: Criar tool get_weather(city) consumindo a OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/5-Back-create-get-weather-tool.md`
- **Resumo:** Implementou a tool `get_weather` chamável pelo LLM, retornando temperatura, descrição, umidade e cidade.

### #4 — BE: Registrar e configurar chave da OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/4-Back-openweather-api-key-http-client.md`
- **Resumo:** Adicionou suporte à OpenWeatherMap no backend — chave, base URL e cliente HTTP (httpx) configurados.

### #3 — BE: Tratar streaming de eventos LLM no backend
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/3-Back-stream-llm-events-sse.md`
- **Resumo:** Implementou o endpoint `POST /api/chat` com `StreamingResponse`, emitindo cada delta de texto do LLM como evento SSE.

### #2 — BE: Implementar tool calling / function calling com o LLM
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/2-Back-implement-llm-tool-calling.md`
- **Resumo:** Implementou a mecânica de tool calling no backend, permitindo que o LLM invoque tools registradas e receba seus resultados.

### #1 — BE: Configurar provedor LLM via .env e pydantic-settings
- **Fechada em:** 2026-07-25
- **Doc:** `a2ui-2026.05.27/docs/issues-plans/1-Back-configure-llm-provider-via-env.md`
- **Resumo:** Integrou a configuração do provedor LLM (OpenAI/Anthropic) ao settings do backend via `pydantic-settings`.
