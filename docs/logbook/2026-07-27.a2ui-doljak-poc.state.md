---
spec: a2ui.ag.ui.state
date: 2026-07-27
repo: doljak-projects/A2UI_A17_Python
repo_url: https://github.com/doljak-projects/A2UI_A17_Python
description: Monorepo A2UI: frontend Angular 17 + Angular Material e backend Python (FastAPI).
generated_by: claude-code / doljak-update-myself
---

# Project State — A2UI · 2026-07-27

## References

| Resource | URL / Path |
|---|---|
| GitHub Repo | https://github.com/doljak-projects/A2UI_A17_Python |
| Local frontend dev | http://localhost:4200 (`npm run fe:start`) |
| Local backend dev | http://localhost:8000 (`npm run be:start`, docs em `/docs`) |
| CLAUDE.md | a2ui-2026.05.27/CLAUDE.md |
| Tutorial A2UI (Parte 1) | a2ui-2026.05.27/docs/tutorial_A2UI/01-ag-ui-sdk-typescript.md |

---

## Session Summary · 2026-07-27 — Sessão 1

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Execução do update completo de estado do projeto (`/doljak-update-myself`): coleta de issues fechadas e em desenvolvimento via `gh`, sincronização de docs de spec e limpeza de labels.

### Combinados para continuar
_(nenhum combinado explícito registrado nesta sessão)_

---

## Session Summary · 2026-07-27 — Sessão 2

> Resumo da sessão — para continuidade em próximas conversas.

### Assuntos tratados
- Leitura do artigo "AG-UI in Practice: The SDK for TypeScript" (Angular Architects) via WebFetch, extraindo conteúdo completo (código, conceitos, exemplo "Flight Weather").
- Criação de `docs/tutorial_A2UI/01-ag-ui-sdk-typescript.md`: tutorial didático (Parte 1) adaptando o exemplo do artigo ao contexto do A2UI (reaproveitando a tool `get_weather` e o `ChatService` já existentes), com 5 passos mapeados 1:1 para issues.
- Criação das issues #32–#36 no GitHub, com nomenclatura `[Back]`/`[Front]` + marcador `-AG-UI-` (mecânica de protocolo/SDK) ou `-A2UI-` (integração específica no projeto). Sem branch/worktree associada — apenas as issues.
- Atualização do `README.md`: seções "AG-UI — Protocolo agêntico" e "A2UI — Frontend agêntico" ganharam blocos "A estudar — Tutorial A2UI Parte 1"; itens de "Frontend — Chat base" (#24, #25, #26) marcados como concluídos (estavam fechados no GitHub mas ainda apareciam pendentes); todas as referências de issue viraram links clicáveis; remoção das seções desatualizadas "Próximos passos" e "Branch ativa".
- Remoção das worktrees `wts/24-worktree-chat-service`, `wts/25-worktree-chat-component`, `wts/26-worktree-chat-routing` e `wts/7-worktree-mcp-server` — todas de issues já fechadas e com branch mesclada em `main`.
- Commit e PR abertos via `/doljak-commit-push-pr`: branch `docs/tutorial-a2ui-part1`, commit `docs: organização etapas A2-UI`, PR [#37](https://github.com/doljak-projects/A2UI_A17_Python/pull/37) (sem issue relacionada, não fecha nenhuma).

### Combinados para continuar
- Avaliar e implementar as issues #32–#36 (Tutorial A2UI Parte 1 — AG-UI SDK TypeScript) quando o trabalho de código for retomado.
- Quando a Parte 1 avançar, documentar a Parte 2 do tutorial (tema já identificado no artigo original: "AG-UI End to End: Connecting Server and Client").

---

## In Development

> Issues com label `developing` — trabalho ativo.

_(nenhuma issue com label `developing` em aberto no momento)_

---

## Closed Issues

> Ordenadas por data de fechamento, mais recente primeiro.

### #26 — FE: Rota /chat e link de navegação na toolbar
- **Fechada em:** 2026-07-25
- **Resumo:** Adiciona rota lazy `/chat` em `app.routes.ts` e link de navegação na toolbar Material do `AppComponent`, com `routerLinkActive`.

### #25 — FE: ChatComponent — layout, estado com Signals e renderização de stream
- **Fechada em:** 2026-07-25
- **Resumo:** Componente standalone de chat com estado gerenciado via Signals (`messages`, `isStreaming`, `error`), renderizando tokens do stream SSE em tempo real.

### #24 — FE: ChatService — consumir POST /api/chat via SSE (fetch streaming)
- **Fechada em:** 2026-07-25
- **Resumo:** Serviço Angular que consome `POST /api/chat` via Fetch API + ReadableStream, parseando eventos SSE tipados (`text_delta`, `message_stop`, `error`).

### #14 — [Backend] Migrate weather integration from OpenWeatherMap to WeatherAPI.com
- **Fechada em:** 2026-07-25
- **Resumo:** Migra cliente HTTP, settings e testes de OpenWeatherMap para WeatherAPI.com, ajustando `get_current_weather` ao endpoint `/current.json`.

### #9 — [Chore] Organize worktree structure and update README
- **Fechada em:** 2026-07-25
- **Resumo:** Padroniza nomenclatura de worktrees (`<numero>-worktree-<tema>`) e documenta o workflow baseado em worktrees no README.

### #8 — BE: Implementar CRUD end-to-end de entidade de exemplo
- **Fechada em:** 2026-07-25
- **Resumo:** Estabelece o padrão de camadas do backend (model, schema, service, router) com CRUD completo de `Conversation`, incluindo migração e testes de integração.

### #7 — BE: Expor tools via MCP Server no FastAPI
- **Fechada em:** 2026-07-25
- **Resumo:** Monta servidor MCP em `app/mcp/server.py` mapeando as tools do `ToolRegistry`, integrado ao lifecycle do FastAPI.

### #6 — BE: Estruturar camada de tools chamáveis pelo LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Cria `ToolRegistry` central com registro, schema JSON e execução uniforme de tools em `app/tools/registry.py`.

### #5 — BE: Criar tool get_weather(city) consumindo a OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa `get_weather(city)` em `app/services/weather.py`, registrada na camada de tools, retornando temperatura, descrição, umidade e cidade.

### #4 — BE: Registrar e configurar chave da OpenWeatherMap
- **Fechada em:** 2026-07-25
- **Resumo:** Adiciona configuração e cliente HTTP (httpx) para a OpenWeatherMap API em `Settings` e `.env.example`.

### #3 — BE: Tratar streaming de eventos LLM no backend
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa `POST /api/chat` com `StreamingResponse`, emitindo cada delta de texto do LLM como evento SSE.

### #2 — BE: Implementar tool calling / function calling com o LLM
- **Fechada em:** 2026-07-25
- **Resumo:** Implementa o contrato de tool calling do LLM: montagem do payload com tools disponíveis e tratamento de `tool_use`/`function_call`.

### #1 — BE: Configurar provedor LLM via .env e pydantic-settings
- **Fechada em:** 2026-07-25
- **Resumo:** Integra configuração do provedor LLM (`llm_provider`, `llm_api_key`, `llm_model` etc.) ao `Settings` via pydantic-settings.
